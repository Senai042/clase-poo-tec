public class comparar {
    //adn y muestra son las arrays
    boolean coin;
    int cant=0;
    //contador para verificar coincidencias

    for (String c; : Adn){
        coin = false;
        for (String Casa : muestra) {
            if (c.equals(Casa)) {
                coin = true;
                break;
            }
        }
        if (coin) {
            cant=cant+1;
    }
}
